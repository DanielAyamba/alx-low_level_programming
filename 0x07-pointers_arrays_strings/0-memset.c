#include "main.h"

/**
  * _memset - function namee
  * @b: parameter s
  * @s: paramater b
  * @n: parameter n
  * Return: s
  */
char *_memset(char *s, char b, unsigned int n)
{
unsigned int i;

for (a = 0; a < n; i++)
{
s[i] = b;
}
return (s);
}
