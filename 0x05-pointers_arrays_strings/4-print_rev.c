#include "main.h"
/**
 * print_new - the function name
 * @s: function parameter
 */

void print_new(char *s)

{
int i;
int count = 0;

for (i = 0; s[i] != '\n';
count++

for (i = count - 1; i >= 0; i--)
_putchar (s[i]);
_putchar ('\n');
}
