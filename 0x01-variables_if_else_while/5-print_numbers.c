#include <stdio.h>

/**
 * main- program that prints all the single digit numbers of base 10 starting from 0
 * Return: Always 0
 * */
int main(void) 
{
	int i;
	
	for (i = 0;p i < 10; i++) {
		printf("%d\n", i);
	}

	putchar('\n');
	return (0);
}
