#include <stdio.h>

/**
 * main - program that prints all single digit numbers of base 10 starting from 0
 * Return: Always 0.
 */

int main(void)
{
    for (int i = 0; i < 10; i++) {
        printf("%d\n", i);
    }

    putchar('\n');
    
    return (0);
}
